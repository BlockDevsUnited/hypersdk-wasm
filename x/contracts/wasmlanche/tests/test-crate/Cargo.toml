[package]
name = "test-crate"
version = "0.1.0"
edition = "2021"

[dependencies]
wasmlanche = { workspace = true }
sdk-macros = { path = "../../../sdk-macros" }

[lib]
crate-type = ["cdylib", "rlib"]

[dev-dependencies]
wasmlanche = { workspace = true, features = ["test"] }
