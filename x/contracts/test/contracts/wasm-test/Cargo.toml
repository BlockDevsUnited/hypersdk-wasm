[package]
name = "wasm-test"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
borsh = { version = "0.10", default-features = false }
bytemuck = { version = "1.17.0", features = ["derive"] }
