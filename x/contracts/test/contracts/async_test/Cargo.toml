[package]
name = "async_test"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
sdk-macros = { path = "../../../sdk-macros" }
wasmlanche = { workspace = true, features = ["bindings"] }

[features]
default = ["bindings"]
bindings = []
