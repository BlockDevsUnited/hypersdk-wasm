[package]
name = "fuel"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmlanche = { path = "../../../wasmlanche" }
sdk-macros = { path = "../../../sdk-macros" }

[build-dependencies]
wasmlanche = { workspace = true, features = ["build"] }
