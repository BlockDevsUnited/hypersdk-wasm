[package]
name = "wasmlanche-build"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = { workspace = true }
anyhow = "1.0"
wasm-opt = { version = "0.116.1", optional = true }

[features]
default = ["wasm-opt"]
wasm-opt = ["dep:wasm-opt"]
