[package]
name = "async_demo"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
contract-sdk = { path = "../../../contract-sdk", features = ["derive"] }
borsh = "0.10.3"
